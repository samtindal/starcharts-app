# Agent Task Briefs

One file per agent. Status (as of 2026-07-07): A1 ✅ · B1 ✅ (astro-core, 34 tests, incl. lunar nodes) · B2/C1 ⚙️ spiked in `prototype/` (drag, tooltips, clickable signs/aspects, legend, unique per-pair teasers) · C2 ⚙️ core math done (natal/transits/synastry in astro-core; UI pending; houses DEFERRED per PLAN P3) · D2 ✅ built, then **PAUSED by owner 2026-07-07** (apps/mcp works, 6 tools verified; precompute/monetization/registry listings all on hold, don't resume without asking) · F1 ✅ (Terraform + CI, not yet applied) · R1 ✅ (docs/competitive-strategy.md) · B2/C1/D1 ✅ shipped in apps/web (Next.js 15: React wheel with drag/tooltips/legend, 358-URL sitemap, 325 aspect-pair pages with computed next-exact dates via on-demand ISR, 12 sign + 12 planet pages, hubs, robots, PWA manifest; production build + smoke tests green 2026-07-07) · E1/E2 pending. **Next up: web Dockerfile + deploy (F1 phase 2), then E1 ads/analytics and E2 QA.** E1 brief was rewritten 2026-07-08 against `docs/ad-monetization-review.md` (network staircase off AdSense, session-depth/internal-linking, sky-today surface, affiliate + email, yield analytics); it now carries soft deps on D1 content depth and a sky-today page. Notes: date-input UI is minimal (native datetime-local); natal/synastry UI (C2) still pending; webpack needs the extensionAlias trick in next.config.mjs for astro-core's .js→.ts imports.

**P1 implemented 2026-07-08 (this pass):** B3 ✅ (Sun/Moon true speed §1, `orbFor` luminary orbs §2, `nodeModel` true-node option §3, `moonPhase` §5, `voidOfCourse` §6, `ascendantMC`/`wholeSignHouses` §8, mode-aware aspect-importance `aspectStrength(asp, mode)` + `rankAspects` §9 (transit/natal/transit-to-natal/synastry); Chiron §4 + quincunx §7 left for P2; +`signWindows` for D3; astro-core now 91 tests green incl. new B3 invariants). Content wired: `orbFor` feeds the aspect pages + `LiveAspectStatus` (no hardcoded orb text); `rankAspects` orders and highlights the clock's aspect list (top 3), the planet-page aspects, and the natal transit list. D3 ✅ (144 `/planets/{point}/{sign}` pages, on-demand ISR, `signWindows` dates + composed copy + static `MiniWheel`, reciprocal hub links, sitemap +144 → ~506). D4 ✅ (`/sky` + `/moon` with live `MoonPanel` SVG phase glyph + VoC line + 30-day VoC table). C2 UI + C3 ✅ (`/rising` + `/chart` `BirthChart`: date/time + "time unknown", offline city type-ahead over a curated dataset w/ IANA tz + manual lat/lon fallback, dependency-free historical local→UTC in `lib/tz.ts`, Big Three panel, `NatalWheel` with Whole Sign ring, transit-to-natal list, localStorage + URL share, GeoNames/IANA attribution in footer). Verified: `npx vitest run` (78 green) and `tsc --noEmit` on apps/web (clean). NOT verified here: full `next build` (the sandbox per-call time limit truncates the webpack compile; run `npm run build` in apps/web to confirm prerender/ISR). Deferred within P1 scope: swap the curated city list for the full GeoNames `cities15000` export; the astro.com end-to-end Ascendant parity literals are left to E2's audit (B3 §8 ships independent horizon-invariant tests instead). D2 still PAUSED, untouched.

**Update 2026-07-12:** the B3 §3 true-node option was dropped (owner decision). It was never surfaced in the UI, so this removed the `nodeModel` param and `trueNodeLongitude` path from `astro-core` plus its §3 tests; lunar nodes are Mean node only. Also re-tiered: natal overlay + transit-to-natal + Big Three moved P1 → P2 (code already shipped; v1 launches with date scrubbing only), and paid one-time reports added as a P2 monetization lever (see `docs/pricing-and-accounts-evaluation.md`).

**Update 2026-07-12 (E3 printable chart PDF):** added a second P2 keepsake lever, a downloadable print-ready PDF of the natal chart wheel that the buyer prints themselves (brief: `e3-printed-charts.md`; PLAN.md §1 P2 row, §3 roster E3, §6 monetization). Reuses the `chart-svg` renderer at print resolution and the paid-reports checkout/legal framing; depends on B2, C2, C3, and the reports checkout foundation. Owner decided 2026-07-12 to sell digital-only (no physical print-on-demand), so it is a pure digital good billed through the reports' Merchant of Record with no separate shipping, tax, or address surface. Not a v1-launch blocker; may slip to P3.

**Update 2026-07-12 (MCP fully deferred out of v1):** owner decision, the MCP server is paused and no part of it launches in P1. Removed from the §8 Definition of Done; D2 marked deferred and dropped from E2's deps in PLAN.md §3; the MCP-only security findings HTTP-2, AUTH-1, CICD-4 re-tagged P2 in `docs/security-audit.md`; the hardening spec's MCP tasks A5/B2/B3 and the MCP half of CICD-3 deferred with it; the mcp Cloud Run service is not served in v1 (see `f1-infra-gcp.md`). Reassess when the service resumes post-launch.

**P1 launch checklist (remaining work to reach the §8 Definition of Done):** with birth-data trimmed and MCP deferred, the gaps between the current build and a shippable v1 are: **E1** (ads + analytics on content pages, `e1-growth-ads.md`), **E2** (QA sign-off: accuracy audit, solver-under-retrograde, Lighthouse ≥90 mobile, cross-browser pointer events, PWA install, `e2-qa-perf.md`), **F1** (finish the Firebase App Hosting setup: create the backend, link GitHub, first rollout, map the domain, `f1-infra-gcp.md` + `docs/firebase-migration-runbook.md`), and **E4** (privacy policy + ad-consent, new `e4-privacy-policy.md`, required once ads ship). The P1 scope-and-hardening spec (`docs/p1-scope-and-hardening-spec.md`) is the scope-trim + security pass and runs alongside these; it does not by itself complete P1.

**Update 2026-07-12 (hosting migrated to Firebase App Hosting):** the web app moved off the hand-built Cloud Run + Terraform + GitHub Actions stack to Firebase App Hosting (managed Cloud Run + CDN, GitHub-connected auto-deploy on `main`). Repo now has `apps/web/apphosting.yaml`, `.firebaserc`, a tests-only `.github/workflows/ci.yml`, and `next.config.mjs` without `output: 'standalone'`; `infra/`, both Dockerfiles, and `deploy.yml` were removed. Edge is Cloudflare proxied in front of App Hosting (chosen 2026-07-12): content routes cached at the free edge respecting ISR TTLs, Bot Fight + one rate-limit rule, egress near $0; this reactivates origin fencing (NET-1), fence the App Hosting origin so the raw URL cannot bypass Cloudflare (fast-follow). F1 rewritten; PLAN.md §2 + §3 updated; the security audit and hardening spec carry supersession notes (WIF/Terraform/Docker findings obsolete, `next.config` security headers still apply). Remaining is the one-time console/CLI setup in `docs/firebase-migration-runbook.md`. MCP stays deferred and unhosted.

**Update 2026-07-12 (security audit re-run for the new architecture):** `docs/security-audit-apphosting.md` is the current audit (Firebase App Hosting + Cloudflare); the old `docs/security-audit.md` is superseded (banner added, kept for history + the deferred MCP findings). The retired stack's IAM/WIF/Terraform/Docker findings are gone; new leading concerns are deploy-access control, account 2FA, and project IAM. Every finding maps to a hardening-spec task (matrix at the bottom of the audit): B1 (headers), B5 (dep scan + pin Actions), B6 (origin fence + Cloudflare ops), and new B8 (deploy access), B9 (project IAM + runtime SA + budget), B10 (secrets + 2FA); Part A still owns DATA-1 birth-data removal (highest pre-launch item, not yet executed). Agent decomposition updated so each task has an owner. Nothing is critical; nothing built yet beyond the config, these are the mitigations to implement.

**Update 2026-07-13 (P1 scope-and-hardening spec, code pass executed):** ran the repo-code half of `docs/p1-scope-and-hardening-spec.md`. Part A (birth-data removal): A0 chose KEEP, so `natal.ts`/`houses.ts` stay in astro-core but their re-exports were pulled from `src/index.ts` (their tests now import the internal module paths directly); A1 deleted `BirthChart.tsx`, `NatalWheel.tsx`, `lib/cities.ts`, `lib/tz.ts`; A2 deleted the `/chart` and `/rising` routes; A3 removed the Rising nav + footer links and trimmed the footer city/timezone clause; A4 dropped `/rising` and `/chart` from the sitemap. A7 verification: the `localStorage|sessionStorage|document.cookie|indexedDB` grep over `apps/web` is now zero, the only remaining "birth" match is Pluto content prose, `npm run typecheck` is clean across all workspaces, and `npm test` is green (astro-core 88, mcp 9). Part B (security): B1 added the `async headers()` block (nosniff, Referrer-Policy, X-Frame-Options DENY, HSTS, and a frame-ancestors/base-uri/object-src CSP; script-src nonce left as the HTTP-1b follow-up); B6 added `apps/web/middleware.ts` (origin fence, 403 on missing/mismatched `x-origin-fence`, health checks allowed, fail-open until the secret is provisioned) plus the `ORIGIN_FENCE_SECRET` RUNTIME `secret:` wiring in `apphosting.yaml`; B5 added `.github/dependabot.yml` (npm + github-actions, weekly) and pinned the two Actions in `ci.yml` to full SHAs; B10 code half added `.firebase/` and `firebase-debug.log` to `.gitignore`. Deferred/out of scope this pass: A5, B2, B3 (paused MCP); B4, B7 (obsolete under App Hosting); the ops halves of B6/B8/B9/B10 (Cloudflare console, GitHub branch protection, project IAM, account 2FA). Note: with the astro-core re-exports gone, `apps/mcp/src/server.ts` now imports `natalChart`/`transitAspects`/`synastry` that no longer resolve; this is the intended consequence of the paused-MCP decision and A5 (deferred) removes those tools when the service resumes. Not verified here: full `next build` (sandbox is arm64 with no registry access for the SWC binary; run `npm run build` in `apps/web` in CI or a normal env).

**Phase 2 briefs added 2026-07-08 (casual-depth review, D3/D4/C3/B3 now implemented above; D5 still open):** B3 ephemeris extensions, C3 rising sign, D3 planet-in-sign, D4 sky-today Moon, D5 content depth (deepen the existing thin aspect/sign/planet pages, D1 shipped them templated and left its "real copy" TODO unfinished; D5 owns it, sharing D3's composed-fragment engine; aspect-opener rewrite + link rail are pre-launch, full 325-page depth is a fast-follow). Rationale from a hobby-astrologer review: the build nails the toy but is missing casual essentials (rising sign / Big Three, moon phase, planet-in-sign content) and has two correctness nits, Sun/Moon `speed` uses mean not true motion (skews the `applying` flag), and orbs are a flat scalar (should be luminary-weighted). PLAN.md updated: rising sign promoted to P1 (Whole Sign only; full house systems stay P3), plus true-node option, moon-phase, void-of-course, planet-in-sign, luminary orbs. Dependency: **B3 lands first**, then {C3, D3, D4} run in parallel. Chiron and minor aspects owner-decided 2026-07-08: Chiron ships via a precomputed JPL Horizons table (1900–2100, opt-in, off by default); minor aspects = quincunx only (toggle, off by default). Both settled in B3 §4/§7. D2 stays PAUSED, B3 only leaves it a TODO comment, does not touch it.

**Update 2026-07-13 (D5 content depth + SEO linking, done):** closed D5 (`d5-content-depth.md`), the last open Phase 2 brief. Aspect pair pages now render the composed multi-paragraph body (`composeAspectParagraphs`) in place of the templated `pairText` mad-libs, a past-and-next exact-date timeline, a new server-rendered aspect-strength-over-time SVG (`aspectStrengthTimeline` in `events.ts`, sampled once per ISR cycle, no client JS), and a full internal-link rail (reverse pair, other four aspects, both current signs, related pairs, type hub); non-canonical pair-order slugs (e.g. `venus-trine-sun`) 301 to the POINTS-ordered canonical slug, which also carries the canonical tag. `composeAspectTeaser` is now the single teaser source for the aspect page, the wheel's aspect table, and the planet page (`pairText`/`PAIR_TEMPLATE` deleted from `lib/content.ts`). Sign and planet pages gained new composed paragraphs (`composeSignParagraphs`, `composePlanetParagraphs` in `interpret.ts`) plus a traditional dignities table on planet pages (`DIGNITIES`, rulership/exaltation, linked to sign pages; nodes correctly carry none). The `/aspects` index links all five aspect types per pair and each type hub lists its 65 pair pages, so every one of the 325 pair pages now has at least two stable, descriptively-anchored inlinks (previously near-orphaned outside the sitemap). `/moon` gained three explanatory sections. A shared `Crumbs` component renders visible breadcrumbs plus `BreadcrumbList` JSON-LD across aspect, sign, planet, planet-in-sign, sky, and moon pages. `interpret.test.ts` extended (12 sign + 12 point uniqueness/depth/cleanliness checks) and a new `timeline.test.ts` added (peak-at-exact-instant, zero-at-window-edge, window-scales-to-pair-speed invariants); `npx vitest run` is green at 106 tests, `npm run typecheck` is clean across all workspaces. Not run here: full `next build` (same arm64/SWC sandbox limitation noted above). Out of scope, left for later: the embedded pre-scrubbed wheel on aspect pages (E1 coordination) and OG images.

**Update 2026-07-13 (E1 ad layer + analytics seam, first pass):** built the network-agnostic ad layer from `docs/adslot-component-spec.md`: `lib/ads/registry.ts` (8-slot registry with reserved sizes), `lib/ads/provider.ts` + `providers/none.ts` + `providers/adsense.ts` (env-selected, `none` is the default so the site is unchanged with ads off), `lib/ads/consent.ts` (GDPR/UK opt-in default-off, honors Global Privacy Control as a standing decline; not an IAB TCF certified CMP, that is a vendor account decision left open), `components/ads/AdSlot.tsx` (reserved-height container, lazy-load via IntersectionObserver, dev-only wheel-adjacency guard, consent-gated mount, engagement-gated refresh on clock slots), and `components/ads/ConsentBanner.tsx`. Wired `content-in-article` + `content-footer` on aspect/sign/planet/planet-in-sign pages, `clock-below-table` + `clock-sidebar` on the clock, `sky-below-fold` on `/sky`; `content-sidebar`, `mobile-anchor`, and the third in-content unit are registered but not yet placed (fast-follow). Added `lib/analytics.ts`, a minimal provider-agnostic events bus (drag/date-jump/aspect-tap wired in `Wheel.tsx`) that also feeds the refresh gate's interaction signal; no vendor sink (GA4/Plausible) chosen yet, events are inert until one is wired in. Added `/about` (AdSense policy review needs a clear human-written statement of what the site is; not in any prior brief, added because it blocks approval) and a minimal `/privacy` (pulled forward from E4 because the consent banner needs somewhere real to link; covers what/why/consent/cookies/GPC/contact, not the full E4 scope: no certified CMP, no CCPA opt-out UI beyond GPC). Added `public/ads.txt` (placeholder, needs the real AdSense publisher id). New `apps/web/test/ads.test.ts` (registry reserved-height invariant, provider-swap default, consent-starts-unresolved); apps/web had no test runner before this, `"test": "vitest run"` added to its `package.json`. Verified: `npm test` green (112 tests across all workspaces), `npm run typecheck` clean, `next build` succeeds (38 routes). Not done: the ad-network migration staircase beyond AdSense, the internal-link rail/embedded-wheel session-depth work, the sky-today build-out, affiliate module, yield dashboard, real AdSense unit ids in the registry, and a certified CMP, all still open per `e1-growth-ads.md`.

**Update 2026-07-13 (E4 privacy policy & consent, done for the code-only scope):** owner decision, the CMP path is Google's own consent tool (Funding Choices, enabled from the AdSense console) rather than a third-party IAB TCF vendor. Added `lib/ads/regions.ts` (EEA/UK/Switzerland as the opt-in-required set, fails safe to regulated when the country is unknown), and made `consent.ts` region-aware: regulated regions keep the existing opt-in gate (banner blocks non-essential storage until a choice), everywhere else (including the US) defaults to consented with a persistent opt-out, matching CCPA/CPRA's opt-out (not opt-in) model. Region comes from a new `app/api/region/route.ts` reading Cloudflare's `cf-ipcountry` header, deliberately isolated to one small dynamic route (`force-dynamic`, `ƒ` in the build output) rather than read via `next/headers` in the root layout, which would have made every statically-generated content page dynamic and defeated the ISR/SEO strategy the whole site depends on; confirmed in `next build` output that all aspect/sign/planet/planet-in-sign pages are still `●` (SSG) after this change. Added `lib/ads/consentMode.ts` (Google Consent Mode v2 default/update signals) and `lib/ads/fundingChoices.ts` (script loader for Funding Choices, gated on `NEXT_PUBLIC_GOOGLE_CMP_ENABLED`, wired to `consent.ts` via its `CONSENT_DATA_READY` callback; a no-op until that env var and `NEXT_PUBLIC_ADSENSE_CLIENT` are both set, so `ConsentBanner` stays the sole gate until the AdSense console step is done). Added a persistent `components/ads/PrivacyChoicesLink.tsx` footer control (CCPA needs an always-available opt-out, not just a first-visit banner); it calls Funding Choices' own `showRevocationMessage()` when active, else reopens the fallback banner. `/privacy` rewritten to describe the region-based consent model, the CMP, and the persistent choices link accurately (previously described a single global opt-in banner). New `apps/web/test/regions.test.ts` (region classification invariants + the `/api/region` route's three cases: regulated, open, no-header-fails-safe). Verified: `npm test` green (119 tests across all workspaces), `npm run typecheck` clean, `next build` succeeds with only `/api/region` newly dynamic. Not done, deliberately left for the owner: actually enabling "Privacy & messaging" in the AdSense console (there is no AdSense account yet), and the full E4 acceptance-criteria walkthrough with a live CMP (untestable without that account). `docs/p1-scope-and-hardening-spec.md`'s ops-only items (Cloudflare console config, GitHub branch protection, account 2FA) are unaffected and still open.

**Update 2026-07-13 (E2 QA & performance, launch gate, first pass):** ran everything automatable without a real device or account. **Accuracy audit (was the named launch blocker, now closed):** queried JPL Horizons (DE441) directly for geocentric apparent ecliptic longitude, 10 bodies x 12 dates spread 1900-2100 (nodes excluded, no Horizons target body, already covered by the mean-node invariant tests), froze the 120 reference values into `packages/astro-core/test/accuracy-audit.test.ts`. Max deviation found: 0.0193° (Moon, 2100-04-04), the 0.1° tolerance from PLAN §7/E2 holds with a wide margin; most points are under 0.002°. **Solver fuzz testing found a real bug, now fixed:** added a seeded-PRNG fuzz test (300 trials, random planet x date 1900-2100 x drag-sized target offset) to `core.test.ts`; it immediately failed on Neptune, tracing to `bracketedSolve` in `solver.ts` capping its search window at 500 days regardless of target distance. For slow outer planets that window only covers a few degrees of actual apparent-longitude range (their annual parallax wobble), so any drag requesting more than that silently failed and the solver returned the input date unchanged, no error, just a stuck wheel, exactly on Neptune/Uranus/Pluto, the "drag Pluto and land in 2247" bodies PLAN.md §4 calls out as the feature's own hook. Fixed with a second, wider search tier that only runs when the local window finds nothing, scaled to the planet's own drift rate (ceilinged at a century); re-verified against 4000 additional fuzz trials across 8 seeds with zero failures, plus all prior solver/regression tests still green. **Perf (Lighthouse, mobile form factor, CPU×4 throttling, the brief's own "throttled CPU×4 in devtools as proxy" method, against a local production build):** home page 98/100/93/100 (perf/a11y/best-practices/SEO), an aspect page 99/96/96/100, CLS 0 on the aspect page (confirms the E1 AdSlot reserved-height design holds under a real audit). Both comfortably clear the ≥90 bar. **Cross-browser pointer events (desktop-engine proxy for the "desktop trio"):** Playwright drag simulation against real Chromium, Firefox, and WebKit engines, all three completed a drag (date input changed), zero console errors under realistic input pacing, and the tooltip-vanishes-on-drag-start rule held on all three; an initial Firefox `setPointerCapture` error traced to the test harness firing a synthetic pointerId Firefox validates more strictly than Chromium/WebKit, confirmed not reproducible with real OS-level mouse input. **Consent gate, live-verified:** zero ad/CMP network requests at any point (no provider configured yet); locally (no Cloudflare `cf-ipcountry` header) `/api/region` fails safe to `regulated` and the banner shows by default; decline/accept correctly persist to `localStorage`; the footer's "manage privacy choices" control correctly reopens the banner after an earlier decision. **Full page crawl:** all 16 routes across every template (clock, aspects index/hub/pair, signs index/detail, planets index/detail/in-sign, sky, about, privacy, sitemap, robots, ads.txt, manifest) return 200 after the E1/E4 wiring. **Found and fixed (owner approved): PWA was not actually installable.** `app/manifest.ts` shipped `icons: []`, empty, and there was no service worker anywhere in the repo, despite PLAN.md §2 naming "manifest + service worker" as the PWA architecture and §8 listing "PWA installable" as a v1 Definition-of-Done item. Lighthouse's automated PWA category no longer exists as of v10+ (Google removed it), so this was caught by direct source inspection, not a score. Fixed: generated `public/icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, and `apple-touch-icon.png` with `sharp` from an inline SVG astrolabe motif (double ring, 12 zodiac ticks, rhumb cross, matching `Wheel.tsx`'s own rendering) using only the theme tokens from `globals.css` (`--bg`, `--gold`, `--gold-bright`), no new colors or off-theme artwork; wired them into `manifest.ts`'s `icons` array and `layout.tsx`'s metadata (`icons.apple`). Added a hand-rolled `public/sw.js` (stale-while-revalidate for same-origin GETs, `/api/*` excluded since those are per-visitor, ad/CMP cross-origin scripts never intercepted) registered from a new `components/ServiceWorkerRegister.tsx`, since Next's chunk filenames are content-hashed per build, a fixed precache manifest isn't viable, this caches the shell on install and opportunistically as pages are visited instead. Live-verified with Playwright: service worker registers and takes control on first load, then a full offline reload of `/` still renders the live wheel with the correct current-device-time chart and zero page errors, confirming "offline shell renders current chart from cached engine" is now real, not aspirational. **Not done, needs a real device or account, not reproducible here:** true iOS Safari and Android Chrome mobile testing (Playwright's engines are a same-family proxy, not the real mobile browsers or their touch-event quirks), the true install-to-homescreen prompt/flow on a real device (icons and manifest are now correct, but the actual OS-level install UI needs a device to see), and real mid-tier-Android drag fps (CPU×4 Lighthouse throttling is the documented proxy and was run, but is not the same as an on-device frame-rate measurement). MCP QA stays deferred with the paused MCP server. Verified throughout: `npm test` green (242 tests across all workspaces: astro-core 220, incl. the 121 new accuracy-audit cases and 2 new solver fuzz/multi-station tests; mcp 9; web 13), `npm run typecheck` clean across all workspaces, `next build` clean.

Note for all agents: import ephemeris symbols from `astro-core/src/ephemeris.ts`, never from `astronomy-engine` directly (CJS/ESM interop differs between vitest and node/tsx).

| ID | File | Depends on |
|---|---|---|
| A1 | a1-architect.md | none |
| B1 | b1-ephemeris-core.md | A1 |
| B2 | b2-chart-renderer.md | A1 |
| C1 | c1-drag-time.md | B1, B2 |
| C2 | c2-date-natal-input.md | B1, B2 |
| D1 | d1-content-seo.md | B1 |
| D2 | d2-mcp-server.md | B1 |
| E1 | e1-growth-ads.md | C1, D1 (+ soft: D1 content depth, sky-today surface) |
| E2 | e2-qa-perf.md | C1, C2, D2 |
| B3 | b3-ephemeris-extensions.md | B1 |
| C3 | c3-rising-sign.md | B3 (§8), C2 |
| D3 | d3-planet-in-sign.md | B1 |
| D4 | d4-sky-today-moon.md | B3 (§5–6) |
| D5 | d5-content-depth.md | D1 (shares fragment engine w/ D3) |
| E3 | e3-printed-charts.md | B2, C2, C3, paid-reports checkout |
| E4 | e4-privacy-policy.md | E1 |
