# A1 — Architect ✅

**Scope:** Monorepo scaffold, shared conventions, task briefs.
**Delivered:** root package.json (npm workspaces), CLAUDE.md, tasks/, packages/astro-core skeleton, prototype/ spike area.
**Deferred to B2/C-row:** `packages/chart-svg` and `apps/*` scaffolds — create when those agents start, so they aren't stale.
